entrypoint : ()->( int:iErrorCode{0} )
{
	/*should print 14*/
	usl::print(sigma(function<(uint:x)->(uint(x*x))>,[1, 2, 3]);
}

/*higher order functions*/
sigma :( (uint)->(uint), [uint] ) -> (uint)
{
	|(f, [])	->	0
	|(f, x::l)	->	f(x) + sigma(f,l)
}