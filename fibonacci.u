entrypoint : ()->( iErrorCode:~i64{0} )
{
	iFib10:i64{fib(10)};
	iErrorCode := iFib10;
	/*usl::print(iFib10);*/
}

/*FIXME only uint*/
fib : (i64:n)->(i64:f)
{
	if(n<1)	{f=1;}
	else	{f=fib(n-1)+fib(n-2);}
}