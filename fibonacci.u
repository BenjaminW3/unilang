()->( ~int:iErrorCode(0) ):entrypoint
{
	int:iFib10(fib(10));
	iErrorCode = iFib10;
	/*usl::print(iFib10);*/
}

/*FIXME only uint*/
(int:n)->(int:f):fib
{
	if(n<1)	{f=1;}
	else	{f=fib(n-1)+fib(n-2);}
}